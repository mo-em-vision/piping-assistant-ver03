# Global unit ontology

Shared unit nodes and `converts_to` edges for all standards packs. Each unit declares a physical dimension key (`pressure`, `length`, `temperature`, `velocity`, …) that links to [`../dimensions/nodes/`](../dimensions/nodes/) dimension nodes.
