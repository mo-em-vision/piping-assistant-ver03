---
id: WF-MAWP
type: workflow
key: mawp_design
name: Maximum Allowable Working Pressure (MAWP)
workflow_class: design_calculation
description: Calculate the maximum allowable working pressure of piping components
  per ASME B31.3.
domain:
- piping
expected_authorities:
- AUTH-ASME-B31.3
- AUTH-ASME-B36.10M
entry_points:
- paragraph: 304.1.2
  role: definition_anchor
expected_parameters:
- PARAM-straight-pipe-section
- PARAM-geometry-input-mode
- PARAM-nominal-pipe-size
- PARAM-pipe-schedule
- PARAM-outside-diameter
- PARAM-actual-wall-thickness
- PARAM-corrosion-allowance
- PARAM-material-specification
- PARAM-design-temperature
- PARAM-joint-category
- PARAM-weld-joint-efficiency
- PARAM-weld-strength-reduction-factor-W
- PARAM-temperature-coefficient-Y
- PARAM-allowable-stress
- PARAM-mawp
goal_expansion:
  root_goal:
    goal_class: calculation_goal
    target_parameter: PARAM-mawp
phases:
- key: expansion_assumptions
  purpose: Confirm assumptions required before graph expansion.
  required_parameters:
  - PARAM-straight-pipe-section
- key: path_decisions
  purpose: Select geometry input mode.
  required_parameters:
  - PARAM-geometry-input-mode
- key: parameter_gathering
  purpose: Collect geometry and material inputs.
  required_parameters:
  - PARAM-nominal-pipe-size
  - PARAM-pipe-schedule
  - PARAM-outside-diameter
  - PARAM-actual-wall-thickness
  - PARAM-corrosion-allowance
  - PARAM-material-specification
  - PARAM-design-temperature
- key: coefficient_resolution
  purpose: Resolve coefficients required by the MAWP equation.
  required_parameters:
  - PARAM-joint-category
  - PARAM-weld-joint-efficiency
  - PARAM-weld-strength-reduction-factor-W
  - PARAM-temperature-coefficient-Y
- key: execution_assumptions
  purpose: Resolve execution assumptions.
  required_parameters: []
- key: definition_equation_completion
  purpose: Complete definition and equation inputs.
  required_parameters: []
applicability:
  applies_to:
  - CONCEPT-pipe
  - CONCEPT-pressure
  - CONCEPT-mawp
report:
  report_type: calculation_report
  required_sections:
  - objective
  - authority_context
  - inputs
  - equations
  - calculations
  - validation
  - warnings
  - conclusion
edges:
- type: depends_on
  target: B313-table-A-1
- type: requires_parameter
  target: WF-PIPE-WALL-THICKNESS
- type: starts_from_paragraph
  target: 304.1.2
- type: may_use_equation
  target: asme_b313_pressure_design_thickness
- type: may_use_equation
  target: asme_b313_mawp_pressure
metadata:
  status: draft
  version: 1
---

Calculate maximum allowable working pressure for piping components.

# Maximum Allowable Working Pressure — Definition

This workflow guides you through calculating the Maximum Allowable Working
Pressure (MAWP) of piping components according to ASME B31.3.

## Governing relationships

1. Pressure design thickness: `t = t_actual - c`
2. Maximum Allowable Working Pressure: `MAWP = 2SEWt / (D - 2Yt)`

# MAWP Calculation

Computes `MAWP = 2SEWt / (D - 2Yt)` per §304.1.2.

# Pressure Design Thickness

Computes `t = t_actual - c` before MAWP calculation.
