## Global rule for ALL steps

Before every Cursor prompt, prepend this:

> “Strictly follow /docs. Do NOT invent new architecture. Do NOT rename components. If something is missing, stub it.”

# STEP 0 — LOCK SCAFFOLD (NO LOGIC) ✅ COMPLETE

### 🎯 Goal

Create folder structure + empty files only.

> Scaffold created. See **§3 Project Structure** in `1. Architecture.md` for the authoritative folder tree.

### Cursor Prompt

> Create the full project folder structure exactly based on SYSTEM_OVERVIEW.md and all /docs files.
> 
> Rules:
> 
> - Do NOT implement any logic
> - Do NOT write business code
> - Only create folders and empty Python files
> - Follow COMPONENT_RESPONSIBILITIES.md strictly
> Output must be a clean runnable Python package skeleton.

### Output you expect

- empty modules
- no logic
- correct architecture baseline

# STEP 1 — DEFINE CORE DATA CLASSES ONLY ✅ COMPLETE

### 🎯 Goal

Implement DATA_MODEL.md ONLY

> Implemented in `models/` using dataclasses. See `models/__init__.py` for exports.

### Cursor Prompt

> Implement ONLY the data models defined in DATA_MODEL.md.
> 
> Rules:
> 
> - No workflows
> - No AI logic
> - No calculations
> - Only pure data structures
> - Use dataclasses or Pydantic (choose one and stay consistent)
> - Ensure models match DATA_MODEL.md exactly
> 
> Create:  
> Task, Workflow, Plan, Step, Input, Output, Result, Report, Error

### Output

- `models/` — clean schema-only layer (dataclasses)
- `models/__init__.py` — public exports

Implemented types (per `data_models.md`):

- `StandardNode`, `GraphVersion`, `GraphEdge`, `EngineeringInput`, `RuleValidation`
- `Task`, `TaskStatus`, `InputConflict`, `Event`, `CalculationResult`, `ReportData`, `AgentContext`

Not implemented (not defined in `data_models.md`):

- `Workflow`, `Plan`, `Step`, `Error`


# STEP 2 — TASK STATE MANAGER ONLY ✅ COMPLETE

### 🎯 Goal

Build state tracking ONLY

> Implemented as `TaskStateManager` in `engine/state/state_manager.py`. Export via `engine.state`.

### Cursor Prompt

> Implement Task State Manager based strictly on DATA_MODEL.md and COMPONENT_RESPONSIBILITIES.md.
> 
> Rules:
> 
> - No workflow logic
> - No AI usage
> - Only CRUD operations on Task state
> - Must be independent module
> 
> Must support:
> 
> - create task
> - update task state
> - store step progress
> - retrieve task

### Output

- `engine/state/state_manager.py` — `TaskStateManager` class
- `engine/state/__init__.py` — public exports

Supported operations:

- create / retrieve / delete task
- update task status (pause, resume, complete, invalidate)
- store inputs, outputs, warnings, active nodes
- store and retrieve step progress
- input conflict detection within and between tasks

# STEP 3 — ROUTER (CLASSIFICATION ONLY) ✅ COMPLETE

### 🎯 Goal

Map request → workflow name

> Implemented in `engine/router.py`. Returns `str | None`. Unmatched requests return `None` for delegation to the AI agent layer (`ai_agent_design.md`).

### Cursor Prompt

> Implement Router module based on COMPONENT_RESPONSIBILITIES.md.
> 
> Rules:
> 
> - ONLY classify requests
> - Do NOT execute workflows
> - Do NOT call AI unless explicitly defined in AI_AGENT_DESIGN.md
> - Return workflow name only
> 
> For now support only:
> 
> - `pipe_wall_thickness_design` workflow (ASME B31.3 scope; standard selection deferred to later steps)

### Output

- `engine/router.py` — `Router` class and `route()` function

Canonical workflow identifier: `pipe_wall_thickness_design`

Behavior:

- matched request → workflow name string
- unmatched request → `None` (caller delegates to AI agent per `ai_agent_design.md`)

### Standards sample pack ✅ IMPLEMENTED

Reference node content for `pipe_wall_thickness_design` exists under `standards/asme_b31.3/`:

| Path | Role |
|------|------|
| `roots/pipe_wall_thickness_design/root.md` | Workflow root entry |
| `nodes/B313-304.1.1/node.md` | Wall thickness calculation node |
| `nodes/B313-304.1.1/formulas/wall_thickness.md` | `t = PD / 2(SEW + PY)` |

See `7. node_structure_design.md` §19 and `1. Architecture.md` §4.1.

  
---  
  
# STEP 6 — WORKFLOW ENGINE  
  
### 🎯 Goal  
  
Create workflow definitions and workflow loading.  
  
### Cursor Prompt  
  
> Implement Workflow Engine based on:  
>  
> - WORKFLOW_DESIGN.md  
> - DESIGN_WORKFLOW_DESIGN.md  
> - DATA_MODEL.md  
>  
> Rules:  
>  
> - Workflows define processes only  
> - Do NOT contain engineering calculations  
> - Do NOT execute steps  
> - Do NOT contain AI logic  
>  
> Must:  
>  
> - load workflow definitions  
> - expose available workflows  
> - provide workflow steps to Planner  
>  
> Create initial workflows:  
>  
> - pipe_wall_thickness_design (root/node content already in `standards/asme_b31.3/`)  
> - piping_design (skeleton only)  
  
### Output  
  
- `workflows/`  
- `workflow_engine.py`  
  


# STEP 8 — AI LAYER (STRICT ISOLATION)

### 🎯 Goal

Add AI but ONLY for interpretation

### Cursor Prompt

> Implement AI layer based on AI_AGENT_DESIGN.md.
> 
> Rules:
> 
> - AI must NOT calculate anything
> - AI must NOT control workflow
> - AI only extracts intent + missing inputs + explanations
> 
> Implement:
> 
> - intent extraction
> - input extraction
> - explanation generator
> 
> Must use PROMPT_LIBRARY.md

### Output

- `ai/agents/`

# STEP 9 — REPORT GENERATOR

### 🎯 Goal

Format final output only

### Cursor Prompt

> Implement Report Generator strictly based on REPORT_GENERATOR_DESIGN.md.
> 
> Rules:
> 
> - No calculations
> - No AI logic
> - ONLY formatting structured results into reports
> 
> Must support:
> 
> - pipe_wall_thickness_design report

### Output

- `reports/generators/pipe_wall_thickness_design_report.py`


# STEP 10 — SYSTEM WIRING (BOOTSTRAP)

### 🎯 Goal

Connect everything

### Cursor Prompt

> Implement system orchestration flow strictly based on SYSTEM_BOOTSTRAP.md.
> 
> Rules:
> 
> - Do NOT introduce new architecture
> - MUST follow exact execution pipeline
> - Wire:  
>     Interface → Router → Planner → Executor → Engineering → Report → Output
> 
> Add a minimal main entry point:
> 
> - main.py or CLI runner

### Output

- `main.py`

# FINAL STEP — INTEGRATION TEST

### Cursor Prompt

> Create a minimal integration test:
> 
> - Input: pipe thickness request
> - Output: full system run
> 
> Rules:
> 
> - No mocking of architecture
> - Must go through full pipeline
> - Must validate SYSTEM_BOOTSTRAP.md flow

