> **EXPLANATORY_DOCUMENTATION** — Boundaries: [`3. component_responsibilities.md`](3.%20component_responsibilities.md). Index: [`docs/core/README.md`](README.md).

# Planner Layer Design

## Purpose

The Planner selects/ranks workflow candidates and orders the **next graph-active unresolved requirement** in phase order. It does not determine active engineering branches, invent dependencies, or author user-facing prompt copy.

Intent classification is owned by **`IntentAgent`** (`ai/agents/intent_agent.py`) upstream.

---

# Terminology

| Term | Meaning |
| --- | --- |
| **Planner Layer** | `engine/planner/` — navigation order, phases, `NavigationPlan` |
| **Planner** | `engine/planner/planner.py` — deterministic; not an LLM |
| **PlannerAgent** | `ai/agents/planner_agent.py` — delegates to `Planner`; LLM fallback on ambiguity only |
| **Graph Engine** | Expansion, `required_user_inputs()`, branch activation |

Pipeline diagram: [`3. component_responsibilities.md`](3.%20component_responsibilities.md).

---

# Owns / must not own

| Owns | Must not |
| --- | --- |
| Workflow candidate ranking from `IntentResult` | Active branches or parameter asks (graph expansion) |
| `NavigationPlan.current_phase`, `phase_missing` | Prompt copy (`engine/messaging/`) |
| Engineering plan / goal projection sync | Formulas, compliance, calculations |
| Phase ordering from workflow `runtime.navigation` | Static lists of all branch parameters |

---

# Phased navigation

`NavigationPhase` order (see [`5. workflow_design.md`](5.%20workflow_design.md) §2):

`expansion_assumptions` → `path_decisions` → `parameter_gathering` → `coefficient_resolution` → `execution_assumptions` → `definition_equation_completion` → `ready`

`build_phased_navigation()` partitions missing fields from graph expansion — not a flat list of all possible inputs.

**Example (pipe wall thickness):** after expansion gate, `phase_missing` might list `design_pressure`, `material_grade`, NPS/D — lookup outputs (E, W, Y, S) are derived, not user-confirmed.

Workflow reference: [`workflows/pipe-wall-thickness.yaml`](../../workflows/pipe-wall-thickness.yaml). Acceptance: [`docs/workflows/pipe_wall_thickness/acceptance_contract.md`](../workflows/pipe_wall_thickness/acceptance_contract.md).

---

# Prompt vs navigation

| Layer | Location |
| --- | --- |
| Which field, which phase | Planner → `NavigationPlan` |
| Wording | `engine/messaging/` (`parameter_input_prompt`, `step_prompt`, `formula_parameter_prompt`) |
| Traversal narration | `GuidanceResolver` — not Planner |

Do not store new prompt strings on `NavigationPlan.questions` / `phase_questions` for greenfield features.

Policy: [`docs/rules.md`](../rules.md) §12, §19.

---

# Workflow discovery example

User: "Check this pipe"

`IntentAgent` → verification intent, piping domain.

`Planner` → candidates include `workflows/pipe-wall-thickness.yaml` (`pipe_wall_thickness_design`); graph expansion determines active path and missing fields.

---

# Implementation

As-built API, modules, and tests: [`engine/planner/README.md`](../../engine/planner/README.md).

Key modules: `planner.py`, `goal_navigation.py`, `plan_phases.py`, `planning_refresh.py` (see audit for current entry points).

---

# Final principle

The Planner behaves like a senior engineer deciding **where to look next** in the graph — not what the engineering answer is.
