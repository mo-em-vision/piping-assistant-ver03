name: pipe_wall_thickness_override
description: User override allows calculation to continue past temperature limit

given:
  inputs:
    design_pressure: { value: 500, unit: psi }
    outside_diameter: { value: 10, unit: in }
    material: { value: SA-106B, unit: dimensionless }
    design_temperature: { value: 900, unit: F }
  validation_overrides:
    - temperature_table_bounds

when:
  user_request: Calculate pipe wall thickness
  intent:
    intent: pipe_wall_thickness_design
    domain: piping
    workflow: pipe_wall_thickness_design
    confidence: 0.95

expected:
  validation:
    plan_status: pass_with_warning

  execution:
    status: completed

  state:
    status: completed
    warnings_present: true

  report:
    status: PASS
    has_calculation_trace: true
