name: pipe_wall_thickness_external_pressure
description: External pressure routes to the external wall thickness stub node

given:
  inputs:
    pressure_loading: { value: external_pressure, unit: dimensionless }
    straight_pipe_section: { value: true, unit: dimensionless }
    joint_category: { value: seamless, unit: dimensionless }
    corrosion_allowance: { value: 0.5, unit: mm }
    d_input_mode: { value: direct_od, unit: dimensionless }
    outside_diameter: { value: 10, unit: in }
    material: { value: SA-106B, unit: dimensionless }
    design_temperature: { value: 200, unit: F }
    external_design_pressure: { value: 15, unit: psi }

when:
  user_request: Calculate pipe wall thickness under external pressure
  intent:
    intent: pipe_wall_thickness_design
    domain: piping
    workflow: pipe_wall_thickness_design
    confidence: 0.95

expected:
  planner:
    selected_root: pipe_wall_thickness_design
    selected_nodes_contains:
      - B313-table-A-1
      - B313-304.1.3
    missing_inputs: []

  execution:
    status: error

  state:
    status: invalidated
